// Example age
let age = 20;

// Using a ternary operator to check voting eligibility
let eligibility = age < 18 ? "You cannot vote" : "You can vote";

// Displaying the result
console.log("Age: " + age);
console.log(eligibility);
